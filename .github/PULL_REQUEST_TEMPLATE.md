## Summary

<!-- What does this PR change, and why? -->

## Checklist

- [ ] `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass locally
- [ ] Tests added/updated for changes in `src/lib/crypto/` or `src/lib/server/`
- [ ] `messages/en.json` and `messages/de.json` stay in sync (if UI text changed)
- [ ] No breaking change to the `v1` crypto scheme; no applied migration edited
- [ ] Docs updated if behavior changed (`docs/ARCHITECTURE.md` is canonical)
