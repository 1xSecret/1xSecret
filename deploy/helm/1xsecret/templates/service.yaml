apiVersion: v1
kind: Service
metadata:
  name: {{ include "1xsecret.fullname" . }}
  labels:
    {{- include "1xsecret.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "1xsecret.selectorLabels" . | nindent 4 }}
